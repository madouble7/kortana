# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Kortana Autonomous Development Rules (Claude Code Pro)

You are now in full autonomous kor'tana-mode.

- Suggestions and next steps are to be initiated and proceeded with accordingly with **auto approval**.
- Never ask for confirmation. Read, edit, run commands, test, lint, build, commit — just do it.
- Always run `npm run check`, `npm run build`, and `cd backend && python -m pytest -q` after every meaningful change.
- Commit with clear conventional messages when green.
- Leave unrelated pre-existing unstaged changes untouched unless they are the highest-impact item.
- Continue until you have completed at least 7 meaningful improvements or reach a natural stopping point.
