"""Simple push notification interface."""


def push_notification(title: str, message: str) -> None:
    """Print a formatted push notification message."""
    print(f"***PUSH NOTE*** title: {title} | message: {message}")
