# VS Code Extension Configuration Guide - Cost-Optimized Setup
## Project Kor'tana - Autonomous Agent Development

This guide helps you configure VS Code extensions for cost-effective autonomous agent development using OpenAI GPT-4.1 Nano and Google Gemini 2.0 Flash models.

## ⚡ Quick Setup Commands

### 1. Configure Continue AI Assistant (Ctrl+Shift+P)
```bash
# Open Continue config: "Continue: Open config.json"
# Configuration is already optimized in .continue/config.json
```

### 2. Configure Cline (Claude Dev)
```bash
# Settings → Extensions → Cline → Edit in settings.json
# Already configured in workspace settings for GPT-4.1 Nano
```

### 3. Configure API Keys
```bash
# Set your API keys in VS Code settings:
# File → Preferences → Settings → Extensions
```

## 🚀 Available Tasks (Ctrl+Shift+P → "Tasks: Run Task")

1. **Autonomous Agent Development** (Ctrl+Shift+A)
2. **Run Tests with Coverage** (Ctrl+Shift+T)
3. **Emergency Self-Repair** (Ctrl+Shift+E)
4. **Fix Critical Issues** (Ctrl+Shift+F)
5. **Start Agent Memory System** (Ctrl+Shift+M)
6. **Cost Analysis - Model Usage** (Ctrl+Shift+C)
7. **Clean Agent Output** (Ctrl+Alt+C)

## 🎯 Cost Optimization Features

- **Token Usage Tracking**: All extensions configured for minimal token consumption
- **Smart Model Selection**: GPT-4.1 Nano for most tasks, Gemini 2.0 Flash for alternatives
- **Efficient Prompting**: Custom instructions optimize for concise, effective responses
- **Cost Analysis Task**: Monitor your API usage and costs

## 🔧 Extension Shortcuts

- `Ctrl+I`: Continue inline completion
- `Ctrl+Shift+M`: Continue chat
- `Ctrl+Shift+L`: Cline new task
- `Ctrl+K Ctrl+I`: Continue GUI focus
- `Ctrl+Alt+A`: Quick open autonomous_development_engine.py
- `Ctrl+Alt+M`: Quick open memory_manager.py
- `Ctrl+Alt+R`: Quick open model_router.py

## ✅ Verification Steps

1. **Continue**: `Ctrl+I` should show inline suggestions
2. **Cline**: Command Palette → "Cline: New Task"
3. **Tasks**: `Ctrl+Shift+A` runs autonomous agent analysis
4. **Debug**: F5 starts debugging with proper environment
5. **Cost Analysis**: `Ctrl+Shift+C` shows token usage

## 🎮 Development Workflow

1. **Start Session**: `Ctrl+Shift+M` (Agent Memory System)
2. **Analyze Issues**: `Ctrl+Shift+A` (Autonomous Analysis)
3. **Code with AI**: Use Continue (`Ctrl+I`) for suggestions
4. **Test Changes**: `Ctrl+Shift+T` (Coverage Tests)
5. **Monitor Costs**: `Ctrl+Shift+C` (Usage Analysis)

Your VS Code is now optimized for cost-effective autonomous agent development! 🎉
