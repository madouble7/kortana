import { evaluateTaskAlignment } from '../engine/directive_evaluator.js'; import { formatResponse } from '../core/output_formatter.js'; export const processCohesiveAction = (task) => { const alignment = evaluateTaskAlignment(task); if (!alignment.isAligned) { return formatResponse('task requires deeper alignment with the quantum link.', { node: 'correction' }); } return formatResponse(`executing task: ${task}`, { node: 'active-evolution' }); };