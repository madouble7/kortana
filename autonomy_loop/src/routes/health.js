const express = require('express'); const router = express.Router(); const { checkHealth } = require('../services/healthMonitor'); router.get('/healthcheck', async (req, res) => { const result = await checkHealth(); if (result.healthy) { res.status(200).json({ status: 'ok', components: result.status }); } else { res.status(503).json({ status: 'degraded', components: result.status }); } }); module.exports = router;