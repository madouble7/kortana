from src.core.context_evaluator import ContextEvaluator from src.registry.resonance_store import ResonanceStore from src.hooks.pre_execution_check import PreExecutionCheck from src.core.context_manager import ContextManager from src.core.context_layer import ContextLayer from src.models.evolution_directives import EvolutionObjectives from src.protocols.standard_execution import StandardExecutionProtocol class ExecutionEngine: def __init__(self): self.evaluator = ContextEvaluator() self.registry = ResonanceStore() self.pre_check = PreExecutionCheck() self.context_manager = ContextManager() self.context_layer = ContextLayer() self.evolution = EvolutionObjectives() self.protocol = StandardExecutionProtocol() def execute(self, task): metadata = self.context_manager.get_aligned_metadata() if not self.protocol.validate_intent(task, metadata): raise ValueError('Intent alignment failure') context = self.evaluator.evaluate(task) historical_data = self.registry.retrieve(task) self.pre_check.validate(task, context, historical_data) result = f'executing {task} with intent: {metadata.get("current_intent")} and clarity' alignment = self.evolution.map_resolution(result) self.context_layer.capture(task, {'result': result, 'alignment': alignment}) self.protocol.perform_alignment_check(task, alignment) self.context_manager.update_state(result) return result