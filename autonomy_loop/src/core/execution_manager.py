from src.logging.metadata_handler import TaskMetadata; from src.analysis.synthesis_engine import SynthesisEngine; import datetime; class ExecutionManager: def __init__(self): self.synthesis_engine = SynthesisEngine(); def execute(self, task_func, task_id, context): metadata = TaskMetadata(task_id, context, datetime.datetime.now().isoformat()); try: result = task_func(); print(metadata.generate_annotation()); return result; except Exception as e: self.synthesis_engine.synthesize(str(e), metadata.to_dict()); print(f"[ERROR_SYNTHESIZED|{metadata.generate_annotation()}]"); raise e