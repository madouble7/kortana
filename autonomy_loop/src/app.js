const express = require('express'); const healthRouter = require('./routes/health'); const requestTracker = require('./middleware/request-tracker'); const { logger } = require('./utils/logger'); const app = express(); app.use(express.json()); app.use(requestTracker); app.use('/', healthRouter); const PORT = process.env.PORT || 3000; app.listen(PORT, () => { logger.info(`kor'tana intelligence running on port ${PORT}`); }); module.exports = app;