const { getSystemState, getAutonomyIndex, getQueueLength } = require('../services/metrics_provider'); const getHealth = async (req, res) => { try { const healthData = { system_state: getSystemState(), autonomy_index: getAutonomyIndex(), queue_length: getQueueLength() }; res.status(200).json(healthData); } catch (error) { res.status(500).json({ error: 'failed to retrieve system metrics' }); } }; module.exports = { getHealth };